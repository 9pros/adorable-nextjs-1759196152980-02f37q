import './globals.css'
import type { Metadata } from 'next'
import { Inter } from 'next/font/google'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'ProClimate HVAC - Award-Winning Heating & Cooling Services',
  description: 'Professional HVAC services with award-winning quality. 24/7 emergency service, installation, maintenance, and repair for all heating and cooling systems.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="scroll-smooth">
      <body className={inter.className}>{children}</body>
    </html>
  )
}